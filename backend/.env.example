# Database Configuration
# For local development, leave DB_TYPE empty or set to 'sqlite'
# For production (Inleed), set to 'mysql'
DB_TYPE=
DB_HOST=localhost
DB_USER=your_database_user
DB_PASSWORD=your_database_password
DB_NAME=mekkompis

# Server Configuration
PORT=3000
NODE_ENV=development

# File Upload
UPLOAD_DIR=../uploads
MAX_FILE_SIZE=10485760

# CORS Configuration
# Development: http://localhost:5173
# Production: your domain (comma-separated for multiple origins)
ALLOWED_ORIGINS=http://localhost:5173

# Authentication (OPTIONAL - leave empty for local development without auth)
# To enable auth in production:
# 1. Generate a strong random secret: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# 2. Generate password hash: Run the hash-password.js script
# JWT_SECRET=your_jwt_secret_here
# AUTH_PASSWORD_HASH=your_bcrypt_password_hash_here
